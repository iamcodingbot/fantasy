class GameConstraint {
    constructor(id, title, color, curr, max ,height) {
        this.id = id;
        this.title = title;
        this.color = color;
        this.curr = curr;
        this.max = max;
        this.height = height;
    }
}

export default GameConstraint;